<!DOCTYPE html>
<html lang="vi">

<head>
    <meta charset="UTF-8">
    <title>Xác Thực 2 Lớp</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

<body class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card shadow">
                <div class="card-header bg-warning text-dark">
                    <h3 class="mb-0">Xác Thực 2 Lớp</h3>
                </div>
                <div class="card-body">
                    <p class="text-muted">Vui lòng nhập mã 6 số từ ứng dụng xác thực của bạn.</p>

                    <% if (error) { %>
                        <div class="alert alert-danger">
                            <%= error %>
                        </div>
                        <% } %>

                            <form action="/2fa/verify" method="POST">
                                <div class="mb-3 text-center">
                                    <input type="text" name="totp" class="form-control form-control-lg text-center"
                                        placeholder="000000" maxlength="6" required autofocus
                                        style="letter-spacing: 10px; font-weight: bold;">
                                </div>
                                <button type="submit" class="btn btn-warning w-100">Xác Nhận</button>
                            </form>
                </div>
                <div class="card-footer text-center">
                    <a href="/login" class="text-decoration-none">Quay lại đăng nhập</a>
                </div>
            </div>
        </div>
    </div>
</body>

</html>