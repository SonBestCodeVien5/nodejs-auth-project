<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard - Khu vực bảo mật</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
    <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="card shadow">
                    <div class="card-header bg-success text-white">
                        <h4 class="mb-0">🎉 Chào mừng đến với Dashboard!</h4>
                    </div>
                    <div class="card-body text-center">
                        
                        <div class="alert alert-info">
                            <h2 class="alert-heading">Xin chào, <%= user.username %>! </h2>
                            <p>Email: <strong><%= user.email %></strong></p>
                            <p>Vai trò: <span class="badge bg-warning text-dark"><%= user.role %></span></p>
                        </div>

                        <hr>
                        <p class="text-muted">Bạn đang ở trong phiên đăng nhập an toàn.</p>
                        
                        <a href="/logout" class="btn btn-danger">Đăng xuất</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>