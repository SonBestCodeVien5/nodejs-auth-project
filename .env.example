# ===== CẤU HÌNH SERVER =====
# Port chạy server (mặc định: 3000)
# Thay đổi: Để port khác nếu cổng 3000 bị chiếm
PORT=3000

# ===== CẤU HÌNH DATABASE =====
# MongoDB Connection URI (đường dẫn kết nối)
# Nếu MongoDB local: mongodb://127.0.0.1:27017/auth_project_db
# Nếu MongoDB Atlas (Cloud): mongodb+srv://username:password@cluster.mongodb.net/dbname
# ⚠️ QUAN TRỌNG: Thay đổi tên database (auth_project_db) nếu cần
MONGO_URI=mongodb://127.0.0.1:27017/auth_project_db

# ===== CẤU HÌNH BẢO MẬT =====
# SESSION_SECRET: Chuỗi bí mật để ký session cookie
# ⚠️ BẮTBUỘC: Phải dài (>20 ký tự) và ngẫu nhiên, KHÔNG dùng "123456" hay "password"
# Mẹo: Dùng random string generator hoặc gõ cái gì đó phức tạp
# VD: son_dep_trai_khong_ai_dep_bang_2025 (đủ dài, khó đoán)
SESSION_SECRET=your_super_secret_key_here_change_me_to_something_long_and_random

# ===== CẤU HÌNH CHO JWT =====
# JWT_SECRET: Chuỗi bí mật để ký JSON Web Tokens
# ⚠️ BẮT BUỘC: Phải dài (>20 ký tự) và ngẫu nhiên
JWT_SECRET=your_jwt_secret_key_here_change_me_to_something_long_and_random

